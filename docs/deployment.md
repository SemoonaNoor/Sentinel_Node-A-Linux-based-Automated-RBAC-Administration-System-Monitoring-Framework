# Deployment Guide
1. Install dependencies: `pip install rich psutil`
2. Ensure Samba is installed: `sudo apt install samba`
3. Make scripts executable: `chmod +x scripts/*.sh`
4. Run the dashboard: `python3 scripts/sentinel_dashboard.py`
